
from setuptools import setup

setup(
    name='JSE',
    packages=['JSE'],
    description='Repository for paper joint subspace estimation (JSE), an iterative algorithm used to separate spurious concept and main-task features in latent spaces of neural networks',
    version='0.1',
    url='anonymous',
    author='anonymous',
    author_email='anonymous',
    keywords=['interpretable ML','Concept erasure']
    )